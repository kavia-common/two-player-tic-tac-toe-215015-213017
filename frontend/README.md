# Frontend Static Analysis Setup

This frontend has ESLint, Prettier, and TypeScript configured.

Scripts:
- npm run lint
- npm run lint:fix
- npm run format
- npm run format:check
- npm run typecheck

Note: After `npm install`, you can run `npm run lint` to analyze the codebase.
